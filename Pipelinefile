import groovy.json.JsonSlurperClassic
import jenkins.model.*
node ("Srikpkash") {
    wrks = env.WORKSPACE
    stage ("Checkout Code") {
        println("Checkout")
    }
    stage ("AppointingTools") {
        script {
            println("The branch revision is ${Revision}")
            println("${env.ApplicationRepositoryURL}")
            println("The build node is ${AgentNode}")
        }
    }
}
