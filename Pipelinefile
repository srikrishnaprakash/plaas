import groovy.json.JsonSlurperClassic
import jenkins.model.*
pipeline {
    node ("Srikpkash") {
        wrks = env.WORKSPACE
        stage ("Checkout Code") {
            
        }
        stage ("AppointingTools") {
            script {
                println("The build node is ${Agent-Node}")
                println("The branch revision is ${BName}")
            }
        }
    }
}